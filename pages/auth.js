import { Fragment } from 'react';
import Head from 'next/head';

const Auth = props => (
  <Fragment>
    <Head>
      <title>Auth</title>
    </Head>
    <h1>Login</h1>
  </Fragment>
);

export default Auth;
